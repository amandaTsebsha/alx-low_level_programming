#include <stdio.h>

int main(){

    int i = 0;
    char alpha = 'a';
    while(i < 10){

        while(alpha < 'z'){
            putchar(alpha);
            alpha++;
        }
        putchar(alpha)
        i++;

    }

    return 0;

}